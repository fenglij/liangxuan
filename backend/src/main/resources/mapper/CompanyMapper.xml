<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liangxuan.company.mapper.CompanyMapper">

    <sql id="companyColumns">
        id, name, website, product, start_time, end_time, work_days, likes, dislikes,
        submitted_by, created_at, updated_at, deleted
    </sql>

    <select id="selectPageByKeyword" resultType="com.liangxuan.company.entity.Company">
        SELECT <include refid="companyColumns"/>
        FROM lx_company
        <where>
            deleted = 0
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%')
                OR product LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY created_at DESC, id DESC
    </select>
</mapper>